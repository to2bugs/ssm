<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.otto.mybatis.mapper.EmpMultiParamMapper">
    <select id="getEmployByIdAndName" resultType="com.otto.mybatis.bean.Emp">
        select * from t_emp where id = #{empId} and emp_name = #{empName}
    </select>
    <select id="getEmployFlux" resultType="com.otto.mybatis.bean.Emp">
        select *
        from t_emp
        where id = #{id} and emp_name = #{ext.name} and age = #{ids[2]} and emp_salary = #{emp.empSalary}
    </select>
</mapper>